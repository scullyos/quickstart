# authorization-ms — your overrides.
#
# Loaded AFTER authorization.defaults.env. Most knobs live one level up
# in quickstart/.env. Use this file for service-specific tweaks.
#
# Examples:
#   CASBIN_SHOULD_LOG=false  # quiet down casbin policy traces
#   DB_LOG_MODE=all          # SQL trace this service only
